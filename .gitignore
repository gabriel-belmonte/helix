# dependencies
node_modules/
.pnpm-store/

# build output
dist/
.turbo/

# site
packages/site/.astro/
packages/site/dist/

# worktrees (kanban task checkouts)
.worktrees/

# internal strategy docs (not for public repo)
internal/
HELIX-AUDIT-COMPARISON.md
HELIX-ROADMAP.md

# compiled binaries (built by release workflow — don't track in git)
packages/cli/helix
packages/cli/helix-dashboard
packages/cli/helix-tui
packages/cli/release.sh

# npm lockfile (we use bun.lock)
package-lock.json

# misc
*.log
.DS_Store

# env / secrets
.env
.env.*
!.env.example

.omo