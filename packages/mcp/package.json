{
  "name": "helix-mcp",
  "version": "0.1.0",
  "description": "MCP (Model Context Protocol) client plugin for Helix — exposes remote MCP tools as local Helix tools.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "bun test test/*.test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "helix-core": "*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  },
  "private": true
}
